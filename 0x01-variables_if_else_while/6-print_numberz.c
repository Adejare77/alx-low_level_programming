#include <stdio.h>

/**
 * main - prints all single digit numbers of base 10
 * starting from 0
 *
 * Return: Always 0 means (success)
 */
int main(void)
{
	int k = 48;

	for (; k < 58; k++)
		putchar(k);
	putchar('\n');

	return (0);
}
